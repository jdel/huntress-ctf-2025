# Steps
* download the file `snooze`
* `file snooze` reveal a 16 bit compressed file
* rename the file by append the suffix `gz` so `mv snooze snooze.gz`
* then `gunzip snooze.gz`
* cat `snooze` reveal the flag