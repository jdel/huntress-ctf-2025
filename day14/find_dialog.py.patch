diff --git a/registryspy/find_dialog.py b/registryspy/find_dialog.py
index 95ac2f1..b1adc00 100644
--- a/registryspy/find_dialog.py
+++ b/registryspy/find_dialog.py
@@ -4,6 +4,7 @@ import PySide6.QtCore as QtCore
 import PySide6.QtWidgets as QtWidgets
 from Registry import Registry
 
+import re
 from . import helpers
 from . import key_tree
 
@@ -149,8 +150,10 @@ class FindDialog(QtWidgets.QDialog):
             if not case_sensitive:
                 text = text.upper()
             if exact_match:
-                if term == text:
-                    return True
+                # hack to regexp
+                return re.search(term, text)
+                # if term == text:
+                #     return True
             else:
                 if term in text:
                     return True
